id: windows.etw.ldap-client
name: Microsoft LDAP Client ETW tracing
resource:
  platform: windows
  family: etw
  mode: managed
  session: DeltaZulu-LDAP-Client
  provider: Microsoft-Windows-LDAP-Client
filter:
  query: |
    Etw
    | where ProviderName =~ "Microsoft-Windows-LDAP-Client"
