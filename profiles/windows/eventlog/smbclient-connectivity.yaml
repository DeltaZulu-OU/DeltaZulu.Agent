id: windows.eventlog.smbclient-connectivity
name: SMB Client Connectivity Event Log
resource:
  platform: windows
  family: eventlog
  channel: Microsoft-Windows-SmbClient/Connectivity
filter:
  query: |
    EventLog
    | where source =~ "Microsoft-Windows-SmbClient/Connectivity"
