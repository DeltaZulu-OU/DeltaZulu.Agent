id: windows.eventlog.system-kernel-pnp
name: System Kernel-PnP driver warning events
mandatory: false
resource:
  platform: windows
  family: eventlog
  channel: System
  provider: Microsoft-Windows-Kernel-PnP
filter:
  query: |
    EventLog
    | where source =~ "System"
    | where EventId in (219)
