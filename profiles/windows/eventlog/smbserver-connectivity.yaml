id: windows.eventlog.smbserver-connectivity
name: SMB Server Connectivity Event Log
resource:
  platform: windows
  family: eventlog
  channel: Microsoft-Windows-SMBServer/Connectivity
filter:
  query: |
    EventLog
    | where source =~ "Microsoft-Windows-SMBServer/Connectivity"
