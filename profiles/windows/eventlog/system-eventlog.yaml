id: windows.eventlog.system-eventlog
name: System Eventlog diagnostic and deletion events
mandatory: false
resource:
  platform: windows
  family: eventlog
  channel: System
  provider: Microsoft-Windows-Eventlog
filter:
  query: |
    EventLog
    | where source =~ "System"
    | where EventId in (104)
