id: windows.eventlog.tpm-wmi
name: TPM WMI Event Log
mandatory: false
resource:
  platform: windows
  family: eventlog
  channel: Microsoft-Windows-TPM-WMI
filter:
  query: |
    EventLog
    | where source =~ "Microsoft-Windows-TPM-WMI"
