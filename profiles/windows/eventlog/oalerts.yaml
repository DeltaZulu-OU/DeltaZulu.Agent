id: windows.eventlog.oalerts
name: Microsoft Office Alerts Event Log
mandatory: false
resource:
  platform: windows
  family: eventlog
  channel: OAlerts
filter:
  query: |
    EventLog
    | where source =~ "OAlerts"
