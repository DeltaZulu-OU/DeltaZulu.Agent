id: windows.eventlog.smbclient-operational
name: SMB Client Operational Event Log
resource:
  platform: windows
  family: eventlog
  channel: Microsoft-Windows-SMBClient/Operational
filter:
  query: |
    EventLog
    | where source =~ "Microsoft-Windows-SMBClient/Operational"
